<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only manifest overlay: diagnostic tooling that must never ship.
     Merged into debug builds only; release manifests are untouched. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="com.granularvolume.debugtools.VoipSimActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
    </application>
</manifest>
